public class Solution {
    public int NumberOfMatches(int n) {
       int total = 0;

       while(n > 1)
       {
           total += (n/2);
           n = n - (n/2);
       }

       return total;
    }
}
