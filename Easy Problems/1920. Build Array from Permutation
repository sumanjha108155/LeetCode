public class Solution {
    public int[] BuildArray(int[] nums) {
        int len = nums.Length;
        int[] arr = new int[len];
        for(int i =0; i<len; i++)
        {
            arr[i] = nums[nums[i]];
        }

        return arr;
    }
}
