public class Solution {
    public int RangeBitwiseAnd(int left, int right) {
       
        while (left < right) {
            right &= (right - 1); // Turn off the rightmost set bit in 'right'
            
        }

        return left & right;
    }
}
